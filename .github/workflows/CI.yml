name: CI

on:
  push:
    branches:
    - master
  pull_request:
    branches:
    - master
  schedule:
  - cron: "0 9 * * *"

jobs:
  test:
    runs-on: ubuntu-latest
    outputs:
      total_jobs: ${{ steps.test.outputs.total_jobs }}
    steps:
    - uses: actions/checkout@v1
    - id: test
      name: Test
      working-directory: ./jobpostings
      run: |
        TOTAL_JOBS=$(go test -timeout 1h github.com/job-hunter-toolkit/job-hunter-toolkit/jobpostings -run TestGetAllJobPostings -v | grep "Total job posings" | awk -F ': ' '{print $4}')
        echo "::set-output name=total_jobs::$TOTAL_JOBS"
  install:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install
      run: go get -u github.com/job-hunter-toolkit/job-hunter-toolkit
