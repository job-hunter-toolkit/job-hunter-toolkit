version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
        environment:
          GO111MODULE: "on"
    steps:
      - checkout
      - run:
          command: go test -parallel 4
          working_directory: ~/project/jobpostings
workflows:
  version: 2
  build_example:
    jobs:
      - build
