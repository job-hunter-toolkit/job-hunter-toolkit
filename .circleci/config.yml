version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.12
        environment:
          GO111MODULE: "on"
    steps:
      - checkout
      - run:
          command: lscpu
          working_directory: ~/project/jobpostings
workflows:
  version: 2
  test_code:
    jobs:
      - test
